spring:
  mvc:
    view:
      prefix: /WEB-INF/
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    platform: h2
    url: jdbc:h2:mem:jalal
  resources:
    static-locations: /src/main/resources/static/
